locations:
  Mainkofen:
    coordinates: { lat: 48.853, lon: 12.985 }
    techs:
      # Demands (explicit column names, all from a single CSV)
      demand_electricity:
        constraints:
          resource: file=electricity_demand_minus_biomass_2023.csv
      
      heat_demand_orc_only:
        constraints:
          resource: file=heat_target_hourly_2023.csv   # -values, sum = -13,615,800

      # Supplies / conversion
      grid_electricity:
        constraints:
          energy_cap_max: 5000   # kW

      biomass:
      supply_biomass:


      # PV at Mainkofen
      pv_parking_1:
      pv_parking_2:
      pv_parking_3:
      pv_parking_4:

      pv_rooftop_1:
      pv_rooftop_2:
      pv_rooftop_3:

      # --- STORAGE (if you added the tech in techs:) ---
      battery_storage:
       # constraints:
          #energy_cap_max: 500     # kW
          #storage_cap_max: 2000   # kWh
